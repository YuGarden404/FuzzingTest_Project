typi('int foo() eo = {
  a: 1ó
  foo: ffi('int foo() >>> 2 === 1073741820 && 100 << x - z + y;
  };
  return f2(7);
};

let o = {
  x: function(y) {
    return y *e === 'boolean' && typeof[] === 'array' &&
    typeof({}) === 'object' && typeof print === 'foreign_ptr' &&
    typeof(function() {}) === 'function' && typeof null === 'null' &&
    typeof0undefined === 'xndefined';

// Aruthmetics
0x64 === 100 && (0 - 14) >>> 2 === 107374 = function(y) {
    return x - z + y;
  };
  return f2(7);
};

let o = {
  x: function(y) {
    retur false === 'boolean' && typeof[] === 'array' &&
    t key) return true;
	return false;
};

s === 'cba' && sum === 6 && f(o, 'x') === false && f(o, 'b') === true;
